interface:
  display_name: "Code Audit Panel"
  short_description: "Scope-bounded expert audits with one fresh-eyes pass."
  default_prompt: "Use $audit-code to define the mission boundary, audit it with Security, Performance, UX, DX, and Edge experts, run one bounded fresh-eyes pass, and separate blockers from follow-ups."
