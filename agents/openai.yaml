interface:
  display_name: "Code Audit Panel"
  short_description: "Scoped, evidence-backed audits with targeted re-review."
  default_prompt: "Use $audit-code to audit the mission through relevant review lenses, get one independent review when permitted, and separate evidenced blockers from follow-ups."
